# editorconfig.org -- if plugin not installed in vs code press ⌘+P then use command `ext install EditorConfig`
root = true

[*]
indent_style = space
indent_size = 2
end_of_line = lf #linefeed!
charset = utf-8
trim_trailing_whitespace = true
insert_final_newline = true

[*.md]
trim_trailing_whitespace = false
